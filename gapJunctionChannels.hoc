objref ks, ksvec, ksgate, ksstates, kstransitions, tobj
{
  ksvec = new Vector()
  ksstates = new List()
  kstransitions = new List()
  ks = new KSChan(0)
}
// gapJunction Density Mechanism
// NonSpecific ohmic ion current
//     i_gapJunction (mA/cm2) = g_gapJunction * (v - e_gapJunction)
{
  ks.name("gapJunction")
  ks.iv_type(0)
  ks.gmax(4e-05)
  ks.erev(-35)
}
// g = gmax
{objref ks, ksvec, ksgate, ksstates, kstransitions, tobj}
